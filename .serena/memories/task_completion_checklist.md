# Чеклист перед сдачей

- Контракты трогали → `./contracts/scripts/validate-specs.sh` + `./contracts/scripts/generate-all.sh`.
- Быстрая проверка → `./scripts/dev/lint.sh` (или `--fix`).
- Тесты по месту: `pytest` / `go test ./...` / frontend tests.
