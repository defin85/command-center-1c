# TODO: RBAC UI — полировка UX/копирайт + рефакторинг "Роли пользователей"

Контекст: `/rbac` -> режим "Назначения" -> вкладки:
- "Доступ к объектам"
- "Роли пользователей"

Цель:
- убрать смешение RU/EN;
- устранить дублирующиеся подсказки;
- выровнять терминологию;
- сделать обязательность `reason` очевидной в UI;
- сделать "Роли пользователей" понятной "с первого открытия" (видно какие роли у каких пользователей и наоборот).

Затронутые места (ориентировочно):
- `frontend/src/pages/RBAC/RBACPage.tsx`
- `frontend/src/pages/RBAC/components/**`
- `frontend/src/api/queries/rbac.ts`
- `orchestrator/apps/api_v2/views/rbac.py`
- `contracts/**` (если потребуется новый/расширенный endpoint)

---

## P0 — обязательно (влияет на понимание/ошибки операторов)

- [x] Вкладка "Роли пользователей": добавить обзор связок без "перекликивания":
  - [x] режимы просмотра: `Пользователь -> Роли` и `Роль -> Пользователи`;
  - [x] по умолчанию показывать `Пользователь -> Роли` (таблица/список);
  - [x] поиск по пользователю + пагинация;
  - [x] фильтр по роли (выбрал роль -> видно пользователей с этой ролью);
  - [x] в строке показывать роли ограниченно: первые 3 + "еще N" (просмотр полного списка через popover/drawer/modal).

- [x] Вкладка "Роли пользователей": безопасное редактирование (чтобы не "снести всё случайно"):
  - [x] action "Изменить" в строке (и/или отдельная форма) открывает модалку назначения ролей;
  - [x] перед применением показывать diff (что добавится/что снимется, итоги N/M);
  - [x] guard для `Replace` с пустым списком ролей: явное подтверждение + summary "будет снято N ролей / останется M".

- [x] API: добавить/расширить "list users with roles" (1 запрос на страницу):
  - [x] параметры: `search`, `role_id` (optional), `limit`, `offset`;
  - [x] ответ: `users[]` с `roles[]` (id+name) + `count/total`;
  - [x] использовать этот endpoint для обоих режимов (`Пользователь -> Роли` и фильтра "пользователи с ролью X");
  - [x] при добавлении нового endpoint обновить `contracts/**`.

- [x] Убрать смешение языков (RU/EN) в RBAC UI:
  - вкладки: `Effective access`, `Audit`, `Infobase Users`;
  - "Роли пользователей": `Assign Roles to User`, `Roles`, `Replace`, `Reason`, `Apply`, `Current roles`;
  - "Доступ к объектам": `Clusters`, `Databases`, `User`, `Role`, `Resource`, `Clear`, `Notes (optional)`, `Reason`, `Grant`.

- [x] Привести терминологию к одному словарю:
  - в "Доступ к объектам" использовать `Пользователь/Группа` (а не `User/Role`);
  - в "Роли пользователей" явно обозначить сущности: пользователь и роли (Django groups), без пересечения терминов с режимом "Роли".

- [x] Сделать обязательность `reason` явно заметной во всех RBAC мутациях (grant/revoke, assign roles, CRUD roles/capabilities, undo):
  - пометка `*` / placeholder "Причина (обязательно)";
  - disable кнопки подтверждения до заполнения;
  - единое сообщение валидации при попытке отправить пустое значение.

---

## P1 — желательно (качество и скорость работы)

- [x] Устранить дублирование подсказок по уровням доступа:
  - выбрать один источник истины (общая подсказка сверху или контекстная по типу ресурса);
  - если оставляем контекстную — сделать её зависимой от выбранного resource type и компактной (например, collapsible/tooltip).

- [x] Привести копирайт подсказок к одному формату:
  - одинаковые формулировки VIEW/OPERATE/MANAGE/ADMIN во всех местах;
  - без повторов и с акцентом на операционные действия (что можно/нельзя).

- [ ] Уменьшить вертикальную "занятость" подсказок:
  - свернуть/скрывать подсказку, когда оператор уже начал работать (или сделать dismissible).

- [ ] Вкладка "Роли пользователей": улучшить обзорность:
  - [ ] показывать подсказку "как читать таблицу" (настройка/скрытие после первого раза);
  - [ ] показать количество ролей у пользователя и количество пользователей в роли (badge).

---

## P2 — опционально (nice-to-have)

- [ ] Добавить короткие helper-тексты для неочевидных полей ("Resource", "Notes"):
  - "Resource" = "Куда выдаём доступ" и т.п.
  - "Notes" = комментарий к назначению (не причина).

- [ ] Проверить консистентность терминов и переводов во всех вкладках RBAC (не только "Доступ к объектам"):
  - "Роли пользователей", "Effective access", "Audit".

- [ ] Добавить e2e/smoke проверку на отсутствие EN-строк в основных точках RBAC (минимально).

- [ ] Вкладка "Роли пользователей": e2e на guard `Replace` с пустыми ролями + проверка diff/summary перед применением.
