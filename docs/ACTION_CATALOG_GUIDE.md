# Action Catalog — decommission notice

Action Catalog больше не поддерживается как platform capability.

- Runtime endpoint `GET /api/v2/ui/action-catalog/` возвращает `404` (`error.code=NOT_FOUND`).
- Legacy UI flow `/templates?surface=action_catalog` удалён из рабочего процесса.

Актуальная инструкция по ручным операциям:
- `docs/MANUAL_OPERATIONS_GUIDE.md`
