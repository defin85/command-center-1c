type: object
description: Request body for multiplex stream subscribe.
properties:
  operation_ids:
    type: array
    items:
      type: string
    description: Operation IDs to subscribe to
required:
  - operation_ids
