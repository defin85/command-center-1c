type: object
properties:
  permissions:
    type: array
    items:
      $ref: ./ArtifactPermission.yaml
  count:
    type: integer
  total:
    type: integer
required:
  - count
  - permissions
  - total
