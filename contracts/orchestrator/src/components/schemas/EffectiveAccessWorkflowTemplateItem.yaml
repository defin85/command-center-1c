type: object
properties:
  template:
    $ref: ./WorkflowTemplateRef.yaml
  level:
    type: string
  source:
    enum:
      - direct
      - group
    type: string
    description: |-
      * `direct` - direct
      * `group` - group
    x-spec-enum-id: 95cc5059aa03e762
  sources:
    type: array
    items:
      $ref: ./EffectiveAccessWorkflowTemplateSourceItem.yaml
required:
  - level
  - source
  - template
