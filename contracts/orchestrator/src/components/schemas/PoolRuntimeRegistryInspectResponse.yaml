type: object
properties:
  contract_version:
    type: string
  entries:
    type: array
    items:
      $ref: ./PoolRuntimeRegistryEntry.yaml
  count:
    type: integer
required:
  - contract_version
  - count
  - entries
