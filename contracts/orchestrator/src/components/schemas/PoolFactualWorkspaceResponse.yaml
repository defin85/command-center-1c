type: object
properties:
  pool_id:
    type: string
    format: uuid
  summary:
    $ref: ./PoolFactualSummary.yaml
  checkpoints:
    type: array
    items:
      $ref: ./PoolFactualRefreshCheckpoint.yaml
  settlements:
    type: array
    items:
      $ref: ./PoolBatch.yaml
  edge_balances:
    type: array
    items:
      $ref: ./PoolFactualBalanceSnapshot.yaml
  review_queue:
    $ref: ./PoolFactualReviewQueue.yaml
required:
  - checkpoints
  - edge_balances
  - pool_id
  - review_queue
  - settlements
  - summary
