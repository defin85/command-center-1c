type: object
properties:
  table_id:
    type: string
  version:
    type: string
  columns:
    type: array
    items:
      $ref: ./TableColumnMetadata.yaml
required:
  - columns
  - table_id
  - version
