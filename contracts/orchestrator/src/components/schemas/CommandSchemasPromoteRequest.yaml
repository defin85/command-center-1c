type: object
properties:
  driver:
    enum:
      - cli
      - ibcmd
    type: string
    description: |-
      * `cli` - cli
      * `ibcmd` - ibcmd
    x-spec-enum-id: 04e91cd3f4c47669
  version:
    type: string
  alias:
    type: string
    default: approved
  reason:
    type: string
required:
  - driver
  - reason
  - version
