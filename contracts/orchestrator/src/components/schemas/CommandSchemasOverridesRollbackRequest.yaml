type: object
properties:
  driver:
    enum:
      - cli
      - ibcmd
    type: string
    description: |-
      * `cli` - cli
      * `ibcmd` - ibcmd
    x-spec-enum-id: 04e91cd3f4c47669
  version:
    type: string
    nullable: true
  version_id:
    type: string
    format: uuid
    nullable: true
  reason:
    type: string
  expected_etag:
    type: string
    nullable: true
required:
  - driver
  - reason
