type: object
properties:
  active_tenant_id:
    type: string
    format: uuid
    nullable: true
  tenants:
    type: array
    items:
      $ref: ./TenantItem.yaml
required:
  - active_tenant_id
  - tenants
