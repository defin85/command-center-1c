type: object
properties:
  count:
    type: integer
    minimum: 0
  limit:
    type: integer
    minimum: 1
  offset:
    type: integer
    minimum: 0
  launches:
    type: array
    items:
      $ref: ./PoolMasterDataSyncLaunch.yaml
required:
  - count
  - limit
  - offset
  - launches
