type: object
properties:
  user_id:
    type: integer
  group_ids:
    type: array
    items:
      type: integer
  mode:
    enum:
      - replace
      - add
      - remove
    type: string
    description: |-
      * `replace` - replace
      * `add` - add
      * `remove` - remove
    x-spec-enum-id: 93f4a33fb738abdd
    default: replace
  reason:
    type: string
required:
  - group_ids
  - reason
  - user_id
