type: object
properties:
  driver:
    enum:
      - cli
      - ibcmd
    type: string
    description: |-
      * `cli` - cli
      * `ibcmd` - ibcmd
    x-spec-enum-id: 04e91cd3f4c47669
  command_id:
    type: string
  catalog:
    type: object
    additionalProperties: {}
    nullable: true
required:
  - command_id
  - driver
