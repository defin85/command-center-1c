type: object
properties:
  items:
    type: array
    items:
      $ref: ./PoolMasterDataDedupeReviewItem.yaml
  count:
    type: integer
    minimum: 0
  meta:
    type: object
    properties:
      limit:
        type: integer
        minimum: 1
      offset:
        type: integer
        minimum: 0
      total:
        type: integer
        minimum: 0
    required:
      - limit
      - offset
      - total
required:
  - items
  - count
  - meta
