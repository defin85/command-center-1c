type: object
description: Deprecated alias for the workflow binding input payload. Use PoolWorkflowBindingInput.yaml in mutating contracts.
properties:
  contract_version:
    type: string
    description: Pool workflow binding contract version.
  binding_id:
    type: string
    description: Stable binding identifier within the pool.
  pool_id:
    type: string
    format: uuid
    description: Enclosing organization pool identifier.
  revision:
    type: integer
    minimum: 1
    description: Server-managed optimistic concurrency revision for update/delete operations.
  workflow:
    $ref: ./WorkflowDefinitionRef.yaml
  decisions:
    type: array
    items:
      $ref: ./PoolWorkflowBindingDecisionRef.yaml
  parameters:
    type: object
    additionalProperties: true
    description: Arbitrary workflow parameter overrides for this binding.
  role_mapping:
    type: object
    additionalProperties:
      type: string
    description: Mapping from workflow roles to concrete pool-scoped targets.
  selector:
    $ref: ./PoolWorkflowBindingSelector.yaml
  effective_from:
    type: string
    format: date
    description: Binding effective start date.
  effective_to:
    type: string
    format: date
    nullable: true
    description: Binding effective end date, inclusive.
  status:
    type: string
    enum:
      - draft
      - active
      - inactive
    description: Binding lifecycle status.
required:
  - workflow
  - effective_from
