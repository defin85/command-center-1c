type: object
description: Error detail structure.
properties:
  code:
    type: string
    description: Error code (e.g., MISSING_PARAMETER)
  message:
    type: string
    description: Human-readable error message
  details:
    type: object
    additionalProperties: {}
    description: Additional error details
required:
  - code
  - message
