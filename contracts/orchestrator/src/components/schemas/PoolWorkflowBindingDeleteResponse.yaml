type: object
properties:
  pool_id:
    type: string
    format: uuid
  workflow_binding:
    $ref: ./PoolWorkflowBindingRead.yaml
  deleted:
    type: boolean
required:
  - pool_id
  - workflow_binding
  - deleted
