type: object
description: Request body for update_database_credentials endpoint.
properties:
  database_id:
    type: string
    description: Database ID to update
  username:
    type: string
  password:
    type: string
    writeOnly: true
  reset:
    type: boolean
    default: false
required:
  - database_id
