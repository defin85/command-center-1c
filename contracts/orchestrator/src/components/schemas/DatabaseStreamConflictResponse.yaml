type: object
description: Conflict response for an already active database stream lease.
properties:
  success:
    type: boolean
    default: false
  error:
    $ref: ./DatabaseStreamConflictErrorDetail.yaml
  request_id:
    type: string
  ui_action_id:
    type: string
required:
  - error
  - request_id
