type: object
properties:
  pool_id:
    type: string
    format: uuid
  workflow_binding:
    $ref: ./PoolWorkflowBinding.yaml
  created:
    type: boolean
required:
  - pool_id
  - workflow_binding
  - created
