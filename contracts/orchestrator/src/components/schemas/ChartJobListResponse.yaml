type: object
properties:
  count:
    type: integer
    minimum: 0
  limit:
    type: integer
    minimum: 1
  offset:
    type: integer
    minimum: 0
  jobs:
    type: array
    items:
      $ref: ./ChartJob.yaml
required:
  - count
  - limit
  - offset
  - jobs

