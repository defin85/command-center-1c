type: object
properties:
  action:
    type: string
    enum:
      - accept_merge
      - choose_survivor
      - mark_distinct
  source_record_id:
    type: string
    format: uuid
  note:
    type: string
  metadata:
    type: object
    additionalProperties: true
required:
  - action
