type: object
properties:
  databases:
    type: array
    items:
      $ref: ./ExtensionsOverviewDatabaseRow.yaml
  count:
    type: integer
  total:
    type: integer
required:
  - count
  - databases
  - total
