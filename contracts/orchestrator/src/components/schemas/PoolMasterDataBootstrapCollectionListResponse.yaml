type: object
properties:
  count:
    type: integer
    minimum: 0
  limit:
    type: integer
    minimum: 1
  offset:
    type: integer
    minimum: 0
  collections:
    type: array
    items:
      $ref: ./PoolMasterDataBootstrapCollection.yaml
required:
  - count
  - limit
  - offset
  - collections
