type: object
description: Request body for update_cluster_credentials endpoint.
properties:
  cluster_id:
    type: string
    format: uuid
  username:
    type: string
  password:
    type: string
    writeOnly: true
  reset:
    type: boolean
    default: false
required:
  - cluster_id
