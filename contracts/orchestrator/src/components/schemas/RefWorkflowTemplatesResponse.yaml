type: object
properties:
  templates:
    type: array
    items:
      $ref: ./WorkflowTemplateRef.yaml
  count:
    type: integer
  total:
    type: integer
required:
  - count
  - templates
  - total
