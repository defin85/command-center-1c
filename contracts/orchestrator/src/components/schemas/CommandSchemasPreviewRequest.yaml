type: object
properties:
  driver:
    enum:
      - cli
      - ibcmd
    type: string
    description: |-
      * `cli` - cli
      * `ibcmd` - ibcmd
    x-spec-enum-id: 04e91cd3f4c47669
  command_id:
    type: string
  mode:
    enum:
      - guided
      - manual
    type: string
    description: |-
      * `guided` - guided
      * `manual` - manual
    x-spec-enum-id: bd50d848143873c7
    default: guided
  connection:
    type: object
    additionalProperties: {}
    nullable: true
  params:
    type: object
    additionalProperties: {}
  additional_args:
    type: array
    items:
      type: string
  catalog:
    type: object
    additionalProperties: {}
    nullable: true
required:
  - command_id
  - driver
