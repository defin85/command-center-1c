type: object
properties:
  database_id:
    type: string
    format: uuid
  cluster_all_eligibility_state:
    type: string
    enum:
      - eligible
      - excluded
      - unconfigured
required:
  - database_id
  - cluster_all_eligibility_state
