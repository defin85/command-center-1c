post:
  operationId: v2_databases_create_ib_user_create
  description: Create a manual infobase user mapping for a database.
  summary: Create infobase user mapping
  tags:
    - v2
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/InfobaseUserMappingCreate.yaml
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/InfobaseUserMappingCreate.yaml
      multipart/form-data:
        schema:
          $ref: ../components/schemas/InfobaseUserMappingCreate.yaml
    required: true
  security:
    - bearerAuth: []
  responses:
    '201':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/InfobaseUserMapping.yaml
      description: ''
    '400':
      description: Invalid request
    '401':
      description: Unauthorized
    '403':
      description: Forbidden
    '404':
      description: Database not found
    '409':
      description: Duplicate entry
  parameters:
    - name: X-CC1C-Tenant-ID
      in: header
      required: false
      schema:
        type: string
        format: uuid
      description: >-
        Optional tenant context selector. If omitted, tenant is resolved via
        user preference or first membership. For staff users, omission may
        return cross-tenant results on some endpoints.
