post:
  operationId: v2_settings_command_schemas_base_update_create
  description: >-
    Upload new base catalog version (v2) and move base alias latest only
    (approved is not touched) (staff-only). Requires reason.


    Supports optimistic concurrency via If-Match header or expected_etag in
    request body.
  summary: Update command schemas base (v2)
  tags:
    - v2
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/CommandSchemasBaseUpdateRequest.yaml
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/CommandSchemasBaseUpdateRequest.yaml
      multipart/form-data:
        schema:
          $ref: ../components/schemas/CommandSchemasBaseUpdateRequest.yaml
    required: true
  security:
    - bearerAuth: []
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/CommandSchemasBaseUpdateResponse.yaml
      description: ''
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
      description: ''
    '401':
      description: Unauthorized
    '409':
      description: Conflict
  parameters:
    - name: X-CC1C-Tenant-ID
      in: header
      required: false
      schema:
        type: string
        format: uuid
      description: >-
        Optional tenant context selector. If omitted, tenant is resolved via
        user preference or first membership. For staff users, omission may
        return cross-tenant results on some endpoints.
