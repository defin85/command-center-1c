post:
  operationId: v2_settings_command_schemas_effective_update_create
  description: >-
    DANGEROUS: Replace effective catalog by uploading it as a new base version
    and resetting overrides (staff-only). Requires reason.


    Supports optimistic concurrency via If-Match header or expected_etag in
    request body.
  summary: Update command schemas effective (v2)
  tags:
    - v2
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/CommandSchemasEffectiveUpdateRequest.yaml
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/CommandSchemasEffectiveUpdateRequest.yaml
      multipart/form-data:
        schema:
          $ref: ../components/schemas/CommandSchemasEffectiveUpdateRequest.yaml
    required: true
  security:
    - bearerAuth: []
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/CommandSchemasEffectiveUpdateResponse.yaml
      description: ''
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
      description: ''
    '401':
      description: Unauthorized
    '409':
      description: Conflict
  parameters:
    - name: X-CC1C-Tenant-ID
      in: header
      required: false
      schema:
        type: string
        format: uuid
      description: >-
        Optional tenant context selector. If omitted, tenant is resolved via
        user preference or first membership. For staff users, omission may
        return cross-tenant results on some endpoints.
