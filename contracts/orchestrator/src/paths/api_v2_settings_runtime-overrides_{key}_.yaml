patch:
  operationId: v2_settings_runtime_overrides_partial_update
  description: >-
    Create/update runtime setting override for current tenant. Staff or
    tenant-admin only.
  summary: Update runtime setting override for tenant
  parameters:
    - in: path
      name: key
      schema:
        type: string
      required: true
    - name: X-CC1C-Tenant-ID
      in: header
      required: false
      schema:
        type: string
        format: uuid
      description: >-
        Optional tenant context selector. If omitted, tenant is resolved via
        user preference or first membership. For staff users, omission may
        return cross-tenant results on some endpoints.
  tags:
    - v2
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/PatchedRuntimeSettingOverrideUpdate.yaml
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/PatchedRuntimeSettingOverrideUpdate.yaml
      multipart/form-data:
        schema:
          $ref: ../components/schemas/PatchedRuntimeSettingOverrideUpdate.yaml
  security:
    - bearerAuth: []
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/RuntimeSettingOverride.yaml
      description: ''
    '400':
      description: Validation error
    '401':
      description: Unauthorized
    '403':
      description: Forbidden
    '404':
      description: Not found
