post:
  operationId: v2_tenants_set_active_create
  description: Persist active tenant for the current user.
  summary: Set active tenant
  tags:
    - v2
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/SetActiveTenantRequest.yaml
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/SetActiveTenantRequest.yaml
      multipart/form-data:
        schema:
          $ref: ../components/schemas/SetActiveTenantRequest.yaml
    required: true
  security:
    - bearerAuth: []
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/SetActiveTenantResponse.yaml
      description: ''
    '400':
      description: Validation error
    '401':
      description: Unauthorized
    '403':
      description: Forbidden
  parameters:
    - name: X-CC1C-Tenant-ID
      in: header
      required: false
      schema:
        type: string
        format: uuid
      description: >-
        Optional tenant context selector. If omitted, tenant is resolved via
        user preference or first membership. For staff users, omission may
        return cross-tenant results on some endpoints.
