# Django и DRF
Django==4.2.25
djangorestframework==3.14.0
django-prometheus>=2.3.1  # Prometheus metrics
django-filter==23.5
drf-spectacular==0.26.5  # OpenAPI/Swagger documentation

# Redis (Celery removed - Go Worker handles task execution)
redis==5.0.1

# HTTP и OData client
requests==2.31.0
urllib3==2.1.0
tenacity==8.2.3  # Для retry logic в OData client
aiohttp>=3.9.0  # Async HTTP client for monitoring

# PostgreSQL
psycopg2-binary==2.9.9

# CORS для frontend
django-cors-headers==4.3.1

# Environment variables
django-environ==0.11.2
python-dotenv==1.0.0

# Encryption
cryptography==41.0.7
django-encrypted-model-fields==0.6.5

# Utilities
pytz==2023.3

# Template Engine
Jinja2==3.1.6

# Workflow Engine
django-pydantic-field>=0.3.0  # Pydantic validation for JSONField
django-fsm-2>=4.0.0  # Finite State Machine for workflow state (community fork)
jsonschema>=4.20.0  # JSON schema validation

# OpenTelemetry Core
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-semantic-conventions>=0.48b0

# OpenTelemetry Exporters
opentelemetry-exporter-otlp>=1.27.0

# OpenTelemetry Auto-instrumentation
opentelemetry-instrumentation-django>=0.48b0
# opentelemetry-instrumentation-celery removed - Celery removed
opentelemetry-instrumentation-redis>=0.48b0
opentelemetry-instrumentation-requests>=0.48b0
opentelemetry-instrumentation-psycopg2>=0.48b0

# Admin Enhancements
django-json-widget>=1.1.1  # JSON Editor widget for Django Admin

# Development
django-extensions==3.2.3
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
ipython==8.17.2

# Load Testing
locust>=2.20.0  # Load testing framework for workflow API

# JWT Authentication
djangorestframework-simplejwt>=5.3.0  # JWT token authentication

# Django Channels (WebSocket support)
channels>=4.0.0  # ASGI + WebSocket support
channels-redis>=4.2.0  # Redis channel layer backend
daphne>=4.0.0  # ASGI server for production

# Static files serving (required for ASGI/Daphne)
whitenoise>=6.6.0  # Serve static files in production/ASGI

# Async testing
pytest-asyncio>=0.23.0  # Async test support
