# Django и DRF
Django==6.0
gunicorn==23.0.0  # WSGI server for systemd production unit
djangorestframework==3.16.1
django-filter==25.2
drf-spectacular==0.29.0  # OpenAPI/Swagger documentation

# Redis (Celery removed - Go Worker handles task execution)
redis==7.1.0

# HTTP и OData client
requests==2.32.5
urllib3==2.6.2
tenacity==9.1.2  # Для retry logic в OData client
aiohttp==3.13.2  # Async HTTP client for monitoring
httpx==0.28.1

# Object storage (MinIO)
minio==7.2.20

# PostgreSQL
psycopg[binary]==3.3.2

# CORS для frontend
django-cors-headers==4.9.0

# Environment variables
django-environ==0.12.0
python-dotenv==1.2.1

# Encryption
cryptography==46.0.3
django-encrypted-model-fields==0.6.5

# Utilities
pytz==2025.2

# Prometheus client (django-prometheus не поддерживает Django 6.0)
prometheus-client==0.23.1

# Template Engine
Jinja2==3.1.6

# Workflow Engine
django-pydantic-field==0.4.1  # Pydantic validation for JSONField
django-fsm-2==4.1.0  # Finite State Machine for workflow state (community fork)
jsonschema==4.25.1  # JSON schema validation

# OpenTelemetry Core
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-semantic-conventions==0.60b1

# OpenTelemetry Exporters
opentelemetry-exporter-otlp==1.39.1

# OpenTelemetry Auto-instrumentation
opentelemetry-instrumentation-django==0.60b1
# opentelemetry-instrumentation-celery removed - Celery removed
opentelemetry-instrumentation-redis==0.60b1
opentelemetry-instrumentation-requests==0.60b1
opentelemetry-instrumentation-psycopg==0.60b1

# Admin Enhancements
django-json-widget==2.1.1  # JSON Editor widget for Django Admin

# Development
django-extensions==4.1
pytest==9.0.2
pytest-django==4.11.1
pytest-cov==7.0.0
pytest-benchmark==5.2.3
ipython==9.8.0
ruff==0.14.10

# Load Testing
locust==2.43.0  # Load testing framework for workflow API

# JWT Authentication
djangorestframework-simplejwt==5.5.1  # JWT token authentication

# Django Channels (WebSocket support)
channels==4.3.2  # ASGI + WebSocket support
channels-redis==4.3.0  # Redis channel layer backend
daphne==4.2.1  # ASGI server for production

# Static files serving (required for ASGI/Daphne)
whitenoise==6.11.0  # Serve static files in production/ASGI

# Async testing
pytest-asyncio==1.3.0  # Async test support
